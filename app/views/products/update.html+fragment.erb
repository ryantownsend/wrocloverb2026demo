<template for="product-dialog-<%= @product.id %>">
  <%= render "product_dialog", product: @product %>
</template>

<template for="product-row-<%= @product.id %>">
  <%= render "product", product: @product %>
</template>
